# Nixpacks build config for Railway
# Pins Node 20+ and uses the standard build/start scripts.

[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm ci --omit=optional || npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server.js"
